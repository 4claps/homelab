kubectl cordon XXX
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

kubectl get nodes

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

TARGET_VERSION='v1.35.6+k3s1'

cd /tmp

rm -f k3s sha256sum-amd64.txt k3s-checksum.txt

curl -fLO \
  "https://github.com/k3s-io/k3s/releases/download/${TARGET_VERSION//+/%2B}/k3s"

curl -fLO \
  "https://github.com/k3s-io/k3s/releases/download/${TARGET_VERSION//+/%2B}/sha256sum-amd64.txt"

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

grep '  k3s$' sha256sum-amd64.txt \
  > k3s-checksum.txt

sha256sum -c k3s-checksum.txt

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

CURRENT_VERSION=$(
  /usr/local/bin/k3s --version |
  awk 'NR==1 {print $3}'
)

echo "$CURRENT_VERSION"

sudo cp -av \
  /usr/local/bin/k3s \
  "/usr/local/bin/k3s-${CURRENT_VERSION//+/-}"

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

sudo systemctl stop k3s

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

sudo install \
  -o root \
  -g root \
  -m 0755 \
  /tmp/k3s \
  /usr/local/bin/k3s

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

/usr/local/bin/k3s --version

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

sudo systemctl start k3s
sudo systemctl status k3s --no-pager

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

kubectl uncordon XXX
